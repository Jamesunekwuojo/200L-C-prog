// Basic structure of  a C program
  // pre processors
  //  main functions 
#include <stdio.h> // processors directives,It handles the function to print out and to input
int main () {
	printf("Hello welcome to CMP 211");

	return 0; // means teminate and it's also check ur prog and make sure 
	 //it's correct'
	
}
